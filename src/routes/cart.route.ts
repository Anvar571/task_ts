import { Router } from "express";

const cartRoute: Router = Router();

// cartRoute.get("/")

// cartRoute.post("/add")

// cartRoute.delete("/delete/:id")

// cartRoute.put("/update/:id")

export default cartRoute